---
interface Props {
  eyebrow: string;
}
const { eyebrow } = Astro.props;
---
<div class="flex items-center gap-6">
  <p class="eyebrow shrink-0">{eyebrow}</p>
  <div class="h-px flex-1 bg-teal/20" aria-hidden="true"></div>
</div>
